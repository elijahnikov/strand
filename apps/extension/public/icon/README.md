# Extension icons

Place PNG icons here sized 16, 32, 48, 96, 128 before shipping:

```
16.png
32.png
48.png
96.png
128.png
```

Then declare them in `wxt.config.ts` under `manifest.icons`. Until real
icons are added, WXT falls back to a placeholder in dev.
