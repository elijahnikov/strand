import type { VerificationEmailProps } from "../templates/verification";

const previewProps: VerificationEmailProps = {
  url: "https://app.omi.local/verify-email?token=preview-token",
  name: "Ada",
};

export default previewProps;
